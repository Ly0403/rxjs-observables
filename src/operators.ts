// import { concat, interval, map, of, range, take, timer } from "rxjs";

export const operators = () => {
//   of(1, 2, 3).subscribe((v) => console.log(v));//123
//   of(1, 2, 3)
//     .pipe(map((v) => v + 1))
//     .subscribe((k) => console.log(k));//234
// console.log("started");
// interval(5000).subscribe((v)=>console.log(v));
// console.log("started");
// timer(5000).subscribe((v)=>console.log(v));
// const timer = interval(5000).pipe(take(4));
// const sequence = range(1, 10);
// const result = concat(timer, sequence);
// result.subscribe(x => console.log(x));
};
